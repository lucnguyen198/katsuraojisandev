import styled from 'styled-components';
import bgPattern from 'assets/img/about-bg-pattern.png';

export const StyledListCard = styled.div`
    padding: 20px 0;
    background: url(${bgPattern}) repeat left top transparent;
`;